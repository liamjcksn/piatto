<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <%= cl_image_tag review.photos[0].key, height: 600, style:"object-fit:cover;", class: 'd-block w-100' %>
    </div>
    <% review.photos.each do |photo| %>
      <% photo_index = review.photos.index(photo) %>
      <% unless photo_index == 0 %>
        <div class="carousel-item">
          <%= cl_image_tag review.photos[photo_index].key, height: 600, style:"object-fit:cover;", class: 'd-block w-100' %>
        </div>
      <% end %>
    <% end %>
    <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>
</div>
